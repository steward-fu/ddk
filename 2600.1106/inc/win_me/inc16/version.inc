OFFICIAL	EQU	1
FINAL	EQU	1
ifdef PLUS98
ifndef WIN32
VERSION	EQU	<"4.90.3000">
endif
VER_PRODUCTVERSION_STR	EQU	<"4.90.30000">
VER_PRODUCTVERSION	EQU	<4,90,0,3000>
VER_PRODUCTVERSION_BUILD	EQU	3000
VER_PRODUCTVERSION_DW	EQU	(045A0000H OR 3000 GT
VER_PRODUCTVERSION_W	EQU	(045AH)
elif defined(NASH) || defined(FROSTING)
ifndef WIN32
VERSION	EQU	<"4.90.3000">
endif
VER_PRODUCTVERSION_STR	EQU	<"4.90.30000">
VER_PRODUCTVERSION	EQU	<4,90,0,3000>
VER_PRODUCTVERSION_BUILD	EQU	3000
VER_PRODUCTVERSION_DW	EQU	(045A0000H OR 3000 GT
VER_PRODUCTVERSION_W	EQU	(045AH)
else
ifndef WIN32
VERSION	EQU	<"4.90.3000">
endif
VER_PRODUCTVERSION_STR	EQU	<"4.90.30000">
VER_PRODUCTVERSION	EQU	<4,90,0,3000>
VER_PRODUCTVERSION_BUILD	EQU	3000
VER_PRODUCTVERSION_DW	EQU	(045A0000H OR 3000)
VER_PRODUCTVERSION_W	EQU	(045AH)
endif
ifndef VS_FF_DEBUG
VS_FF_DEBUG	EQU	0
endif
ifndef DEBUG
VER_DEBUG	EQU	0
else
VER_DEBUG	EQU	VS_FF_DEBUG
endif
ifndef VS_FF_PRIVATEBUILD
VS_FF_PRIVATEBUILD	EQU	0
endif
ifndef OFFICIAL
VER_PRIVATEBUILD	EQU	VS_FF_PRIVATEBUILD
else
VER_PRIVATEBUILD	EQU	0
endif
ifndef VS_FF_PRERELEASE
VS_FF_PRERELEASE	EQU	0
endif
ifndef FINAL
VER_PRERELEASE	EQU	VS_FF_PRERELEASE
else
VER_PRERELEASE	EQU	0
endif
VER_COPYRIGHT_CURRENT_YEAR	EQU	<"2000">
VER_FILEFLAGSMASK	EQU	<VS_FFI_FILEFLAGSMASK>
VER_FILEOS	EQU	<VOS_DOS_WINDOWS16>
VER_FILEFLAGS	EQU	(VER_PRIVATEBUILD OR VER_PRERELEASE OR VER_DEBUG)
VER_COMPANYNAME_STR	EQU	<"Microsoft Corporation0">
ifndef NASH
VER_PRODUCTNAME_STR	EQU	<"Microsoft(R) Windows(R) Millennium Operating System0">
else
VER_PRODUCTNAME_STR	EQU	<"Microsoft(R) Windows(R) Millennium Operating System0">
endif
VER_LEGALTRADEMARKS_STR	EQU	<"Microsoft(R) is a registered trademark of Microsoft Corporation. Windows(R) is a trademark of Microsoft Corporation.0">
