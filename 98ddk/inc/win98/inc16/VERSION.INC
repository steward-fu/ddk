OFFICIAL	EQU	1
FINAL	EQU	1
ifdef PLUS98
ifndef WIN32
VERSION	EQU	<"4.80.1998">
endif
VER_PRODUCTVERSION_STR	EQU	<"4.80.19980">
VER_PRODUCTVERSION	EQU	<4,80,0,1998>
VER_PRODUCTVERSION_BUILD	EQU	1998
VER_PRODUCTVERSION_DW	EQU	(04500000H OR 1998 GT
VER_PRODUCTVERSION_W	EQU	(0450H)
elif defined(NASH) || defined(FROSTING)
ifndef WIN32
VERSION	EQU	<"4.70.1998">
endif
VER_PRODUCTVERSION_STR	EQU	<"4.70.19980">
VER_PRODUCTVERSION	EQU	<4,70,0,1998>
VER_PRODUCTVERSION_BUILD	EQU	1998
VER_PRODUCTVERSION_DW	EQU	(04460000H OR 1998 GT
VER_PRODUCTVERSION_W	EQU	(0446H)
else
ifndef WIN32
VERSION	EQU	<"4.10.1998">
endif
VER_PRODUCTVERSION_STR	EQU	<"4.10.19980">
VER_PRODUCTVERSION	EQU	<4,10,0,1998>
VER_PRODUCTVERSION_BUILD	EQU	1998
VER_PRODUCTVERSION_DW	EQU	(040A0000H OR 1998)
VER_PRODUCTVERSION_W	EQU	(040AH)
endif
ifndef VS_FF_DEBUG
VS_FF_DEBUG	EQU	0
endif
ifndef DEBUG
VER_DEBUG	EQU	0
else
VER_DEBUG	EQU	VS_FF_DEBUG
endif
ifndef VS_FF_PRIVATEBUILD
VS_FF_PRIVATEBUILD	EQU	0
endif
ifndef OFFICIAL
VER_PRIVATEBUILD	EQU	VS_FF_PRIVATEBUILD
else
VER_PRIVATEBUILD	EQU	0
endif
ifndef VS_FF_PRERELEASE
VS_FF_PRERELEASE	EQU	0
endif
ifndef FINAL
VER_PRERELEASE	EQU	VS_FF_PRERELEASE
else
VER_PRERELEASE	EQU	0
endif
VER_COPYRIGHT_CURRENT_YEAR	EQU	<"1998">
VER_FILEFLAGSMASK	EQU	<VS_FFI_FILEFLAGSMASK>
VER_FILEOS	EQU	<VOS_DOS_WINDOWS16>
VER_FILEFLAGS	EQU	(VER_PRIVATEBUILD OR VER_PRERELEASE OR VER_DEBUG)
VER_COMPANYNAME_STR	EQU	<"Microsoft Corporation0">
ifndef NASH
VER_PRODUCTNAME_STR	EQU	<"Microsoft(R) Windows(R) Operating System0">
else
VER_PRODUCTNAME_STR	EQU	<"Microsoft(R)  Plus!  for  Windows(R)  980">
endif
VER_LEGALTRADEMARKS_STR	EQU	<"Microsoft(R) is a registered trademark of Microsoft Corporation. Windows(R) is a trademark of Microsoft Corporation.0">
